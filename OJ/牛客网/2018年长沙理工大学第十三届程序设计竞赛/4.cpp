#include <bits/stdc++.h>
using namespace std;

const int maxn = 220;
int G[maxn][maxn];
queue<int> q;


signed main() {
	freopen("input.txt", "r", stdin);
	for(int i = 0; i < 5; i++) {
		for(int j = 0; j < 5; j++) {
			scanf("%d", &G[i][j]);

			
		}
	}
	return 0;
}