#include<cstdio>
int month[] = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31} ;
int YY, MM, DD ;//???????????????
int DaysFromBegin = 0 ;

int IsRun (int y)    //?????????????????????
{
    if (((y % 4 == 0) && (y % 100 != 0 ))
        || (y % 400 == 0))
        return 1 ;
    return 0 ;
}


int days (int yy, int mm, int dd)    //??????yy-mm-dd?????????1???1????????????
{
    int day = 0, i ;
    for ( i = 1 ; i < mm ; i++)
        day += month[i] ;
    
    if (IsRun (yy) && mm >= 3)
        day ++ ;
    day += dd ;
    return day ; 
}


int fun (int yy, int mm, int dd)    //??????yy-mm-dd??????????????????????????????
{
    
    int day = 0;
    int i ;
    //?????????
    for ( i = YY ; i < yy ; i++)
        if (IsRun (i))    day += 366 ;
        else    day += 365 ;
        
        //??????yy-mm-dd?????????1???1????????????
        day += days (yy, mm, dd) ;
        
        //????????????????????????1???1????????????
        day -= DaysFromBegin ;
        
        return day % 7 ;    
}

int main ()
{
    
    int yy, mm, dd ;
    int wk ;
//    freopen ("in.txt", "r", stdin) ;
    scanf ("%d-%d-%d", &YY, &MM, &DD) ;
    DaysFromBegin = days (YY, MM, DD) ;
    
    while (EOF != scanf ("%d-%d-%d", &yy, &mm, &dd))
    {
        wk = fun (yy, mm, dd) ;
        switch (wk)
        {
        case 0: 
        case 1:    printf ("学英语\n") ; break ;
        case 2: 
        case 3:    printf ("学数学\n") ; break ;
        case 4: 
        case 5:    printf ("做AC\n") ; break ;
        case 6:    printf ("网游\n") ; break ;
        default :break ;    
        }
    }
    
    return 0 ;
}