#include <stdio.h>
int main()
{
    int a, b, c, d, t;
    int sum,
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d%d%d", &a, &b, &c);
        printf("%d %d %d %d %d\n", )
    }
    return 0;
}
