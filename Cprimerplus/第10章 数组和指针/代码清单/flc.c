#include <stdio.h>
#define COLS 4
int sum2d(const int ar[]COLS, int rows);
int sum(const int ar[], int n);
int main(void)
{
    int total1, total2, total3;
    int * p1;
    int
}
/*
    符合字面量通俗地理解为一个数组，但是作为函数的参数时不需要先创建，可以直接当做参数使用
    但是因为它是匿名地，所以如果想再次使用它就需要拿一个地址来记录它。
*/
